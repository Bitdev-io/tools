package tools

import (
    "testing"
)

func TestList(t *testing.T) {
    tests := []struct {
        name string
    }{
        // TODO: Add test cases.
        {
            name: "List",
        },
    }
    // TODO: actually check something to expect
    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) {
        })
    }
}
